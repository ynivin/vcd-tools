class Dumps:

    def __init__(self, id = '', dumps = {}):
        self.id     = id
        self.dumps  = dumps

    def get_id(self):
        return id

    def add_dumps(self, new_dumps):
        dumps.update(new_dumps)
